<!-- Page Content -->
        <div id="page-wrapper">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header">Dashboard</h1>
                    </div>.
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            <i class="fa fa-bell fa-fw"></i>
                             Rubric List
                        </div>
                        <button ng-click="count = count + 1 * 5">Click me!</button>
                            <p>{[{ count }]}</p>


                        <div ng-repeat="courses in courses"> </div>

                        <!-- /.panel-heading -->
                      
                         
                        <!-- /.panel-body -->
                    </div>

                    <!-- /.col-lg-12 -->
                </div>
                <!-- /.row -->
            </div>
            <!-- /.container-fluid -->
        </div>
        <!-- /#page-wrapper -->