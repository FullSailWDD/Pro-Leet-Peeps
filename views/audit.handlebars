<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1>{[{data.major}]}</h1>
                <div id="topBlock" ng-click="actionLeave()">
                x
                </div>
            <h2>{[{ data.rubric.name }]}</h2>
            <div ng-repeat="section in sectionSplit">
                {[{ section }]}
            <div ng-repeat="grade in gradeSplit">
                
            <button>{[{ grade }]}</button>
                </div>
            </div>
                    
        </div>
    </div>
</div>
