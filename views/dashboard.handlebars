<div id="page-wrapper">
    <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Course Dashboard</h1>

    <table class="table">
      <thead>
        <tr>
          <th>Major</th>         
          <th>Rubric</th>
          <th>Description</th>
          <th>Action</th>
          <th>&nbsp;</th>
        </tr>
      </thead>
        <tr ng-repeat="data in courses">
          <td>{[{data.major}]}</td>
          <td>{[{data.rubric.name}]}</td>
          <td> Description </td>
          <td><button class="btn btn-danger" ng-click="remove(courses._id)">Edit</button></td>
          <td><button class="btn btn-success" ng-click="remove(courses._id)">Remove</button></td>


        </tr>

      <!--   <tr>
          <td ng-repeat="major in courses">{[{major |json}]}</td>
        </tr>

        <tr>
          <td ng-repeat="major in courses">{[{major |json}]}</td>
        </tr>

        <tr>
          <td ng-repeat="major in courses">{[{major |json}]}</td>
        </tr> -->
        </tr>
    </table>
            </div>
        </div>
    </div>
</div>

</div>
